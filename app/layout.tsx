import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

export const metadata: Metadata = {
  title: 'Samba Shampoo - Cabelos Brasileiros, Beleza Natural',
  description: 'Descubra a linha completa de shampoos Samba, desenvolvida especialmente para cabelos brasileiros. Produtos naturais que cuidam, hidratam e realçam a beleza dos seus cachos.',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
